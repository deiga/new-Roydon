- content_for :content do
  %nav#shop_navi
    - unless session[:top_category_ids].nil?
      %ul
        - session[:top_category_ids].each do |top_category_id|
          %li= category_link(top_category_id)
        %li.stretch
  %section#sub-navigation-sidebar
    = render 'shop/layouts/search'
    = render 'shop/layouts/category_menu'
  %section#product-view
    = content_for?(:shop_content) ? yield(:shop_content) : yield
  %section#sidebar
    = render 'shop/layouts/shopping_cart'
    = render 'shop/layouts/advertisement'
= render :template => 'layouts/application'