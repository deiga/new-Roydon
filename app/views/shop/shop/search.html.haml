%section#search-results
  %h2= t 'shop.search.title'
  - unless @products.blank?
    #paginator
      = paginate @products, remote: true
    #products
      = render @products, cart: @cart
  - else
    %p Ei tuloksia haulle
