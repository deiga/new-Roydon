%tr{ id: "#{show.date}" }
  %td{ headers: 'show_time' }
    = show.format_date
  %td{ headers: 'show_name' }
    %a{ href: "#{show.url}" }
      = show.title
  %td{ headers: 'show_place' }
    = show.location